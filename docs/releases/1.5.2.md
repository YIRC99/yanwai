# 言外 1.5.2

- 修复「帮我回」最多 100 条实际却只取首屏十几条的问题：生成/重写时在后台查询当前聊天的最近本机文字历史，最多 100 条、48,000 UTF-16 单位。
- 复用微信已打开的 WCDB 主库，先与页面文字锚点核对，再执行按聊天参数化的只读限量查询；不打开数据库文件、不获取密钥、不写入或关闭宿主数据库。
- 上下文入口显示实际条数、来源、时间范围和原文。历史未就绪或核对失败时显示「仅页面消息」，不会宣称读到了完整历史；模型收到相同的来源及片段限制。
- 保留旧建议、理由和上下文重开恢复；关闭、离开或切换聊天取消未完成请求。历史读取后、上传前再次核对会话、许可和模型配置。Jev 的轻量情绪上下文不变。

验证：169 项离线单测通过（零失败/错误/跳过），Debug 构建与 Lint 通过（0 错误、125 警告）。新增覆盖 17 条页面消息扩展为最近 100 条、真实短历史、错误账户锚点、跨聊天结果、不可读库回退、旧句柄跳过、参数化限量查询、取消、群聊成员、长文本上限及模型来源标记。审查中已将可能误导为完整历史的标记改为仅描述页面来源。

版本 1.5.2 / 21，已覆盖安装到已连接手机。未启动或操作微信 UI；宿主实际数据库接口、时间范围及新旧消息内容仍需用户重启微信后验收。仅读取本机保留的文字记录，清理掉或只在其他设备上的聊天不可补齐；当前入口仍需要可识别的页面文字锚点。验收步骤见 [回复建议](../REPLY_ASSISTANT.md)。仅本地提交，不推送或发布。
