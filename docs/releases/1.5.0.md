# 言外 1.5.0

- 回复模型预设豆包、小米 MiMo、DeepSeek、OpenAI GPT、GLM、Kimi，支持自定义 OpenAI 兼容地址，并提供官方 Key 申请入口。
- 每家独立保存 Key 与模型，切换保留本页未保存输入，升级时保留原来的回复配置。
- 获取真实模型列表后在下拉框选择或输入筛选；继续支持手动模型 ID、豆包推理接入点。豆包和 GLM 提供明确标注来源的参考模型。
- 模型列表获取与示例回复检测分开，列表可见不等于账户有调用权限。错误 Key、不支持列表、超时均有恢复路径。
- 情绪判断、手动生成许可、聊天上下文和消息发送流程保持原有行为。

验证：153 项离线单测通过，Debug APK 构建成功；Lint 无错误，125 项警告（包括硬编码文案和既有适配提示）。APK 元数据为 1.5.0 / 19。模型接口通过本地 HTTP 服务验证鉴权、路径、返回解析、错误、重定向阻止与响应大小限制；配置迁移、独立保存及请求取消有回归覆盖。

未使用用户真实 Key 调用各供应商，未自动启动或测试手机界面。真实账户模型获取、回复质量与下拉交互由用户按 [验收步骤](../REPLY_ASSISTANT.md#手机验收) 检查。只做本地提交，不推送或发布。
