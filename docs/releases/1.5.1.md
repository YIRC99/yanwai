# 言外 1.5.1

- 撤掉聊天输入框上方常驻横条，尝试在微信「＋」功能面板内加入紧凑图标入口，保留原生项目和分页；长按标题「分析」菜单新增备用入口。
- 回复抽屉增加明确的回复卡片、细边框与轻阴影，理由默认显示；补充想法有清晰输入边界与聚焦态，底部复制和填入并排，减少说明文字。
- 关闭重开恢复上次成功回复、理由和补充想法，不自动重发模型请求；取消或重写失败保留旧内容。有新消息只提示手动更新。
- 最近 12 个聊天分别在微信进程内保留结果，不写入磁盘，进程结束清空。查看历史消息或语音输入模式下也可重开复制。
- 撤销成功同步当前抽屉草稿基线；重复填入同一句建议不覆盖原稿撤销记录。

验证：157 项离线单测通过，Debug 构建和 Lint 检查通过；版本 1.5.1 / 20。历史缓存、会话隔离、取消请求、失败保留和容量淘汰有回归覆盖。静态复核已修复撤销后再次填入的状态问题。

未自动启动或操作手机 UI。加号面板记录为本机历史隐藏视图，展开后图标高度、分页、键盘及浅深色效果仍需用户实机验收；不把编译通过当作微信布局适配通过。依据与实现边界见 [加号入口调研](../REPLY_PLUS_RESEARCH.md)。仅本地提交，不推送或发布。
