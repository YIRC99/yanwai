# 构建产物
build/
.gradle/
*.apk
*.aab

# 本机路径，不该入库（每台机器的 SDK 位置不同）
local.properties

# 旧版 KSP 元数据不再使用；传统入口 assets/xposed_init 现在纳入版本管理。
app/src/main/resources/META-INF/yukihookapi_init

# 当前使用传统 Xposed 入口，作用域在 Manifest 与资源数组中声明。

# IDE
.idea/
*.iml
.DS_Store

# 日志/临时文件
*.log
build.txt
test.txt

# 个人构建密钥和签名材料不入库。
*.keystore
*.jks
keystore.properties
.kotlin/
jev.local.properties
verification/
