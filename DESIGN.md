# 言外界面规范

面向拿起手机、想快速确认助手是否可用的微信用户。白天与夜间均跟随系统主题，以原生字体和控件为基础，重点放在状态与下一步动作。

- 当前首页页签为「聊天分析」。先展示 JEV 情绪连接状态，再选择意图线路「JEV 决策模型 · 快速判断 / 智能分析 · 细致解读」。选项下只保留当前线路说明；LLM 表单仅选中时展示，切回 JEV 不丢失本页 LLM 草稿。配置保存才切换微信实际线路，未保存明确提示。JEV 已配置且选择 LLM 时收起 JEV 详细表单，仍显示情绪来源和修改入口。两个连接分别检测，不把 JEV 通过当成 LLM 通过。
- 分析卡为 12dp 圆角、12dp 横向内边距、13sp 正文、深色半透明底和微弱浅边框，文字完全不透明。两条线路统一使用约 24dp 静态情绪圆环，环内为百分比、旁边保留情绪名；全部 10 种情绪固定配色，概率为 0 的情绪不显示、不占位，非零小概率仍显示 <1%，按可用宽度自动换行。大字模式随字号增大，单项超过可用宽度时退回可换行文字，保留读屏内容。智能分析等待解读时也显示相同圆环，不显示长条。首行是 yanwai 和实际版本；上下内边距 7dp、附加行距 1dp，去除段落空行，智能分析每项优先 20–40 字短句，末尾为「仅供参考」；意图/可能在意/情绪倾向标题以浅青色加粗区分。背景采样在当前宿主窗口内存中降采样、共享，每次宿主绘制最多每 180ms 更新一次；不创建定时任务，最后一张卡移除时释放。背景绘制失败退回透明色层，不对文字施加模糊。设备布局、壁纸与滚动性能由用户实机验收。

- 微信设置入口沿用微信原生列表行：「插件」标题下显示「言外设置」及原生箭头。已有 WeKit 等插件时排在其后，不重复增加分组标题，不覆盖原有列表和点击；没有插件时自行显示「插件」标题。随列表滚动、深浅主题及字体缩放，不再固定到底部。完整微信日志继续由聊天页长按「分析」导出。

- 身份菜单末项为「自定义身份」。选中后在选择器下方展开单行输入框并聚焦，提示「填写对方身份，如：前同事、相亲对象」；最多 40 字，不额外弹窗。填写后选择器显示实际身份，过长时省略但读屏保留全文；空白时生成按钮提示先填写。切回预设收起输入框，隐藏文字不影响预设身份。

- 「生成回复 / 找找话题」并排，补充输入框示例分别说明回复语气与生日、爱好、近况。找话题后标题只显示一次，下面按 1–3 句展示可点选的短消息，每次只复制选中的一句；保留已看话题数量及剩余数量。原位置按钮变为「换个话题」，耗尽时说明将请求新的一组。「更简短」放在回复结果下，话题模式不展示。
- 加载反馈固定在面板标题下，不随结果滚动：26dp 转动圆环和明确的读取/生成/找话题文案，浅深色沿用主题，关闭、失败、取消或离开时停止；系统关闭动画时仍显示静态加载状态。旧结果保持可见，生成期间锁定使用操作。

- 顶部固定分段切换「聊天分析 / 回复建议 / 关于言外」。前两个页面显示对应功能的概览、配置、权限、检测状态和微信运行记录；「关于言外」集中展示遇到问题、版本与更新、联系作者、免费与开源。切换不清空未保存输入，不把 Jev 检测状态套到回复模型，页面重建恢复所选页签。
- 回复页使用同一原生字号、24dp 概览圆角和单一青绿色主操作。地址、Key、模型和手动生成许可在保存操作之前，来源说明有可点击原项目和完整许可。
- 回复配置按供应商、Key、获取模型、选择模型、检测回复排列。预设展示只读地址，自定义显示可编辑地址；模型使用可输入筛选的原生下拉框，列表为空时仍可手填。实时列表与参考目录用文字区分，获取列表不显示“回复检测通过”。每家独立保存，切换保留本页未保存输入。
- 微信内「帮我回」收进「＋」展开面板的紧凑图标行，撤除输入区上方横条及额外消息留白；保留原有功能和分页。长按「分析」菜单提供备用入口。
- 抽屉使用 24dp 圆角、较弱遮罩；首行并排两个选择器，未指定身份时直接显示「选择对方身份」，右侧「参考最近 N 条」提供 30/50/100 条。下方是可选补充想法和主色生成按钮。新面板约 360dp，结果面板最多 560dp，均受屏高限制。打开、选身份和选条数不触发模型；首次打开及选条数只预读本机历史。建议按条展示，正文 16sp，有条目序号与选中状态；理由按需展开。底部固定单个主按钮「复制这条」，提示「复制后，请自行粘贴到聊天框发送」，保持 48dp 触控区域、系统字体缩放及浅深色。
- 已生成内容关闭后重开直接恢复身份、建议、理由、生成时的补充想法和选中条目；生成、重写失败均不闪空旧回复。切换身份后旧建议标为上次结果，重新生成前禁用使用，切回原身份可继续。复制不自动推进选择，不写入微信输入框，也不显示填入或撤销操作。上下文入口对应所展示回复的实际证据；有新消息仅提示手动更新。
- 移除常驻的「选好后点击生成 / 页面消息」与时间范围行。读完且达到所选条数时不增加说明；加载、失败或不足时只显示一条简短状态。选择器菜单可查看当前已读取内容，回复标题「查看依据」展示该组建议生成时的实际消息、来源与时间，二者不会相互覆盖。读取失败明确标为页面片段，生成按钮写实际条数；选定数量不冒充实际数量。

- 首屏是使用概览：一句结论、原因、一个下一步按钮；模型检测和微信运行记录分开，历史记录不代表在线。
- 最顶部标题为「言外 v版本号」，名称旁始终显示 BuildConfig 的实际安装版本。桌面图标使用原创生成的暖白聊天气泡与玉绿色引号，固定深青绿色背景；不与系统深浅主题联动。
- 日常开关紧随概览。模型设置按渠道、Key、保存并检测的顺序；预设渠道隐藏不可编辑的地址，自定义显示完整字段。
- 启用教程、渠道教程和排查详情按需展开；日志导出与版本更新统一从「关于言外」进入。
- 低饱和青绿色只用于主要动作和成功状态，琥珀色表示待完成，红色表示明确失败。每个颜色必须有文字解释。
- 浅色背景 #F3F6F4，主文字 #182B24，主色 #20685C；深色背景 #101916，主文字 #E1EEE6，主色 #8CD7C0。Android 资源使用 sRGB 色值。
- 标题 26sp，分区标题 20sp，正文 14sp，辅助文字 12sp。章节间距 28–32dp，相关内容间距 8–12dp，页面边距 20dp，触控目标至少 48dp。
- 使用概览是唯一强调卡片，其余表单与操作直接分区，避免层层套卡。长文字可换行，沿用系统字体缩放，支持浅深色。
- 表单改动明确标记“未保存”；检测中展示进度并锁定配置输入；成功与失败行内显示，失败给重试路径。不会把保存成功标成检测通过。

本轮视觉与手机交互由用户实机验收；本地只验证状态判定及构建。
