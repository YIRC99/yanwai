# 言外界面规范

面向拿起手机、想快速确认助手是否可用的微信用户。白天与夜间均跟随系统主题，以原生字体和控件为基础，重点放在状态与下一步动作。

- 顶部固定分段切换「情绪判断 / 回复建议 / 关于言外」。前两个页面显示对应功能的概览、配置、权限、检测状态和微信运行记录；「关于言外」集中展示遇到问题、版本与更新、联系作者、免费与开源。切换不清空未保存输入，不把 Jev 检测状态套到回复模型，页面重建恢复所选页签。
- 回复页使用同一原生字号、24dp 概览圆角和单一青绿色主操作。地址、Key、模型和手动生成许可在保存操作之前，来源说明有可点击原项目和完整许可。
- 回复配置按供应商、Key、获取模型、选择模型、检测回复排列。预设展示只读地址，自定义显示可编辑地址；模型使用可输入筛选的原生下拉框，列表为空时仍可手填。实时列表与参考目录用文字区分，获取列表不显示“回复检测通过”。每家独立保存，切换保留本页未保存输入。
- 微信内「帮我回」收进「＋」展开面板的紧凑图标行，撤除输入区上方横条及额外消息留白；保留原有功能和分页。长按「分析」菜单提供备用入口。
- 抽屉使用 24dp 圆角、较弱遮罩；首行并排两个选择器，未指定身份时直接显示「选择对方身份」，右侧「参考最近 N 条」提供 30/50/100 条。下方是可选补充想法和主色生成按钮。新面板约 360dp，结果面板最多 560dp，均受屏高限制。打开、选身份和选条数不触发模型；首次打开及选条数只预读本机历史。建议按条展示，正文 16sp，有条目序号与选中状态；理由按需展开。底部固定「复制这条 / 填入第 N 条」，保持 48dp 触控区域、系统字体缩放及浅深色。
- 已生成内容关闭后重开直接恢复身份、建议、理由、生成时的补充想法和选中条目；生成、重写失败均不闪空旧回复。切换身份后旧建议标为上次结果，重新生成前禁用使用，切回原身份可继续。填入一条后默认选中下一条，未发送或清空的上一条不被覆盖；撤销成功回选被撤销的条目。上下文入口对应所展示回复的实际证据；有新消息仅提示手动更新，撤销入口位于抽屉底部。
- 移除常驻的「选好后点击生成 / 页面消息」与时间范围行。读完且达到所选条数时不增加说明；加载、失败或不足时只显示一条简短状态。选择器菜单可查看当前已读取内容，回复标题「查看依据」展示该组建议生成时的实际消息、来源与时间，二者不会相互覆盖。读取失败明确标为页面片段，生成按钮写实际条数；选定数量不冒充实际数量。

- 首屏是使用概览：一句结论、原因、一个下一步按钮；模型检测和微信运行记录分开，历史记录不代表在线。
- 最顶部标题为「言外 v版本号」，名称旁始终显示 BuildConfig 的实际安装版本。桌面图标使用原创生成的暖白聊天气泡与玉绿色引号，固定深青绿色背景；不与系统深浅主题联动。
- 日常开关紧随概览。模型设置按渠道、Key、保存并检测的顺序；预设渠道隐藏不可编辑的地址，自定义显示完整字段。
- 启用教程、渠道教程和排查详情按需展开；日志导出与版本更新统一从「关于言外」进入。
- 低饱和青绿色只用于主要动作和成功状态，琥珀色表示待完成，红色表示明确失败。每个颜色必须有文字解释。
- 浅色背景 #F3F6F4，主文字 #182B24，主色 #20685C；深色背景 #101916，主文字 #E1EEE6，主色 #8CD7C0。Android 资源使用 sRGB 色值。
- 标题 26sp，分区标题 20sp，正文 14sp，辅助文字 12sp。章节间距 28–32dp，相关内容间距 8–12dp，页面边距 20dp，触控目标至少 48dp。
- 使用概览是唯一强调卡片，其余表单与操作直接分区，避免层层套卡。长文字可换行，沿用系统字体缩放，支持浅深色。
- 表单改动明确标记“未保存”；检测中展示进度并锁定配置输入；成功与失败行内显示，失败给重试路径。不会把保存成功标成检测通过。

本轮视觉与手机交互由用户实机验收；本地只验证状态判定及构建。
