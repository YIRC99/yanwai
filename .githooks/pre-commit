#!/bin/sh
if command -v pwsh >/dev/null 2>&1; then
  exec pwsh -NoProfile -File tools/check-version.ps1 -Staged
else
  exec powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/check-version.ps1 -Staged
fi
