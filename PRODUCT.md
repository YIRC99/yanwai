# 微信情绪助手

## Register

product

## Users

供本人在 Android 手机的微信聊天中使用。已有 Xposed/LSPosed 环境，安装 APK 后在模块管理器启用，聊天时直接查看提示，不需要每次打开设置页。

## Product Purpose

第一阶段做让人愿意持续使用的「闲聊潜台词助手」：结合前文解读事件、情绪和可能的潜台词，直接展示概率及下一步动作。原型参考是用户提供的聊天气泡下方 Jev 提示。只用 Jev 分层选择内置模板和动作，不生成自由文本，不自动回复。沿用 jev_demo 的模型配置，个人版在构建时内置配置，不要求用户填写地址或密钥。

当前内置 8 类闲聊场景、32 套解读模板。第一层保留情绪判断，增加针对对象、交流行为、求建议、待兑现约定、我方过错和新话题判断；程序过滤使用条件，第二层独立选择解读和下一步动作。分享工作学习经历不直接排除，抱怨第三方不当成双方冲突，结束旧事件不等于结束聊天。移除好感线索和原话引用，保留情绪概率，建议保持一句具体动作。

## Brand Personality

简单、直白、克制。沿用 Android 原生控件和系统浅深色，页面服务于启用与排查。

## Anti-references

避免原版截图中标题压住内容、难读的紫色按钮文字、要求填写模型参数，以及把本进程空缓存误报成微信分析状态。

## Design Principles

- 默认准备好个人模型，必要操作只用开关。
- 状态有证据才显示成功，模型连通不代表微信已经注入。
- 保留原消息行和 ViewHolder，在文字气泡下追加独立分析卡，标题栏「绘制」开关控制显示；不覆盖原文，离开、重绑和关闭绘制时清理卡片。
- 对方纯文本为分析目标，最多带入之前 10 条消息中的合格双方纯文本；单条超过 1000 字符直接跳过。模型结果不跨会话或不同上下文复用。
- 首行只显示 Jev 和应用版本；下方保留情绪概率，解读和建议分别在有可靠选择时显示。没有贴合的解读仍可显示合适动作，两者都不确定时仅保留情绪。不显示「先别急着猜」等占位话，不填通用建议。
- 仅分析可见的对方纯文本，按微信消息类型过滤，跳过所有媒体和无法确认类型的消息。
- 调试信息折叠收起；不把接口协议和密钥放在首页。

## Accessibility & Inclusion

保留系统字体缩放、48dp 触控区域、浅深色适配；内容避开状态栏、标题栏和底部手势区域。颜色之外保留文字说明。
