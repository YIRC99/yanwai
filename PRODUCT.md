# 微信情绪助手

## Register

product

## Users

供本人在 Android 手机的微信聊天中使用。已有 Xposed/LSPosed 环境，安装 APK 后在模块管理器启用，聊天时直接查看提示，不需要每次打开设置页。

## Product Purpose

第一阶段做让人愿意持续使用的「闲聊潜台词助手」：结合前文解读事件、情绪和好感线索，用简短、有趣但不武断的分析卡提示这句话可能在表达什么、下一步怎么接。原型参考是用户提供的聊天气泡下方 Jev 提示。只用 Jev 分层选择内置模板和动作，不生成自由文本，不自动回复。沿用 jev_demo 的模型配置，个人版在构建时内置配置，不要求用户填写地址或密钥。

当前内置 8 类闲聊场景、32 套模板。第一层识别场景与对话进展，第二层选择具体分析卡并判断备选解释，代码组合结果。关注「等解释→等行动→已接受→收尾」的变化，避免重复建议。好感只描述当前可观察的互动意愿，不宣称知道对方真实想法，也不把生气等同于关系疏远。

## Brand Personality

简单、直白、克制。沿用 Android 原生控件和系统浅深色，页面服务于启用与排查。

## Anti-references

避免原版截图中标题压住内容、难读的紫色按钮文字、要求填写模型参数，以及把本进程空缓存误报成微信分析状态。

## Design Principles

- 默认准备好个人模型，必要操作只用开关。
- 状态有证据才显示成功，模型连通不代表微信已经注入。
- 保留原消息行和 ViewHolder，在文字气泡下追加独立分析卡，标题栏「绘制」开关控制显示；不覆盖原文，离开、重绑和关闭绘制时清理卡片。
- 对方纯文本为分析目标，最多带入之前 10 条消息中的合格双方纯文本；单条超过 1000 字符直接跳过。模型结果不跨会话或不同上下文复用。
- 卡片展示最相关的一个问题、主要备选解释的模型概率、情绪/互动线索及一个动作。找不到贴合模板或信心不足时说清线索不足，不硬套戏剧化结论。
- 仅分析可见的对方纯文本，按微信消息类型过滤，跳过所有媒体和无法确认类型的消息。
- 调试信息折叠收起；不把接口协议和密钥放在首页。

## Accessibility & Inclusion

保留系统字体缩放、48dp 触控区域、浅深色适配；内容避开状态栏、标题栏和底部手势区域。颜色之外保留文字说明。
