# 言外 · Yanwai

## Register

product

## Users

供有 Xposed/LSPosed 环境的 Android 微信用户使用。安装 APK 后填写自己的 API 地址和 Key，在模块管理器启用，聊天时直接查看提示，不需要每次打开设置页。

## Product Purpose

「帮我回」打开后先选对方身份、填写可选补充想法，再手动生成。支持未指定、暗恋、暧昧、恋人、朋友、长辈、弟弟妹妹、其他家人、同事和其他关系，选用狗头军师对应原始资料；原始知识完整保留。建议按自然语意输出 1–6 条连续短消息，通常 1–3 条，每次仅复制或填入选中的一条，用户自己发送。语气词依据真实聊天习惯与亲密度，不机械添加。成功结果连同身份和选中条目只在进程内按聊天限量记住。

设置首页使用「情绪判断 / 回复建议 / 关于言外」三个页签。问题排查、版本更新、作者联系方式和免费开源说明集中到「关于言外」，功能页保留各自的配置与微信使用状态。

1.5.2 修复回复上下文只覆盖首次加载的十几条消息：主动生成时在后台读取当前聊天最近最多 100 条本机文字记录，保留发送人、时间和 48,000 字符上限。页面消息用于确认当前聊天及核对数据库记录；不匹配或历史不可读时保留页面片段并明确提示。抽屉显示实际条数、来源及时间范围，旧建议重开不重新查询。Jev 情绪判断仍用原先的轻量上下文。

1.5.1 优化回复交互：入口收进聊天「＋」功能面板；撤掉常驻横条，保留长按入口。抽屉明确区分回复卡片、默认展示的理由和补充想法输入框，复制/填入并排固定。关闭后保留同一聊天的上次成功建议，重开无需重发模型请求；进程内有界保存最近 12 个聊天，不持久化聊天。

1.5.0 简化回复模型配置：预设豆包、小米 MiMo、DeepSeek、OpenAI GPT、GLM、Kimi，独立记住每家 Key 和模型，保留自定义接口。支持实时获取候选模型、下拉筛选选择、手动填写与真实示例回复检测；不支持列表的服务可以使用明确标注的文档参考模型。模型可见不等于账户可调用，最终以回复检测为准。

1.4.0 新增「帮我回」：情绪判断和回复建议在顶部切换，配置和检测状态独立。回复使用用户配置的 OpenAI 兼容 Chat Completions LLM，首次需开启手动生成许可。输入框上方按钮手动触发，参考最近最多 100 条已加载聊天及发送人、时间；底部面板支持换一句、简短改写、补充目标、理由、复制和填入输入框，保留原稿并可撤销，不自动发送。回复逻辑来自 goutoujunshi，完整打包其原始知识并保留 MIT 许可，界面明确来源。以下 Jev 分层选择规则继续适用于情绪判断，回复功能使用独立生成协议。

「言外」是闲聊潜台词助手：结合前文解读事件、情绪和可能的潜台词，直接展示概率及下一步动作。原型参考是用户提供的聊天气泡下方 Jev 提示。只用 Jev 分层选择内置模板和动作，不生成自由文本，不自动回复。1.1.0 支持 Jev 官方、OpenRouter、Vercel AI Gateway 与自定义 Jev 兼容服务。选择渠道后自动匹配地址和模型，展示简短的 Key 申请教程、申请入口与官方接入文档；构建不包含用户密钥，各渠道的 Key 分开保存。

当前内置 43 套解读模板：原有 8 类场景 32 套，加 8 套通用意图和 3 套状态解释。两轮携带相同的文字、发送者、时间和时间间隔；第一轮独立判断情绪、交流行为、对象、话题延续等，第二轮复核情绪和意图，并核验各动作前提。明确拒绝、暂停或告别优先限制建议和解读。保持模型不确定出口，时间不自动消除或延续情绪。

1.1.2 增加 GitHub 正式 Release 更新提醒：只在打开设置页时匿名检查，发现高版本且有 APK 时提示，由用户前往 GitHub 下载覆盖安装；保留手动检查入口，不新增后台常驻服务，不把模型 Key 或聊天内容发送给 GitHub。

## Brand Personality

1.2.0 汇总设置页与日志改进，在页面底部提供作者微信二维码、微信号 YIRC99 和开源地址。项目完全免费，无会员或付费解锁；第三方模型服务按其自身规则计费。二维码随 APK 打包，README 复用同一张原图。

1.1.4 重排独立设置页：首屏显示使用概览、模型检测和微信运行记录，主按钮根据当前状态给出下一步；日常开关、模型配置、微信启用步骤、日志排查和版本更新按使用顺序排列。预设渠道隐藏已匹配的地址，首次教程按需展开，状态文字配合语义颜色。沿用已有设置、检测和日志链路，不把模型检测通过当成微信当前在线。

简单、直白、克制。沿用 Android 原生控件和系统浅深色，页面服务于启用与排查。

## Anti-references

避免原版截图中标题压住内容、难读的紫色按钮文字、要求填写模型参数，以及把本进程空缓存误报成微信分析状态。

## Design Principles

- 首次选择渠道，按页面教程申请并保存对应 Key；预设自动填写地址和模型，自定义才显示模型名。使用过程中主要操作开关。
- 状态有证据才显示成功，模型连通不代表微信已经注入。
- 保留原消息行和 ViewHolder，在文字气泡下追加独立分析卡。标题栏「分析」开关按联系人/群聊记忆，默认关闭；关闭后隐藏全部卡片并清除当前聊天手动选择。长按对方文字的「翻译意图」可在关闭时单独分析，结果仍显示在消息下方。离开或重绑时清理卡片，不覆盖原文。
- 独立 APP 的名称「言外」旁展示当前版本，桌面使用专属气泡/引号图标，便于识别已安装版本。每次本地提交同步升版。
- 对方纯文本为分析目标，从之前最多 80 个页面位置收集最近 24 条合格双方文字，总量最多 12000 字符；单条超过 1000 字符跳过并记录缺口。时间、时区和来源变化也会使缓存失效，结果不跨会话复用。仍不代表已读取完整历史。
- 首行只显示 Jev 和应用版本；下方保留情绪概率，解读和建议分别在有可靠选择时显示。没有贴合的解读仍可显示合适动作，两者都不确定时仅保留情绪。不显示「先别急着猜」等占位话，不填通用建议。
- 仅分析可见的对方纯文本，按微信消息类型过滤，跳过所有媒体和无法确认类型的消息。
- 调试信息折叠收起；API Key 默认掩码，可切换显示，不在状态和日志中输出。说明消息会发送到用户填写的接口。

## Accessibility & Inclusion

保留系统字体缩放、48dp 触控区域、浅深色适配；内容避开状态栏、标题栏和底部手势区域。颜色之外保留文字说明。
